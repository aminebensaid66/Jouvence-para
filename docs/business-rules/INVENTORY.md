# Inventory rules

`InventoryPolicy` is the single launch-policy definition for low-stock threshold (3), WooCommerce hold time (30 minutes), one-unit online safety buffer, backorders disabled and non-disclosure of exact stock quantities. Add-to-cart validation uses the safety-buffer amount and emits only a generic availability error.
